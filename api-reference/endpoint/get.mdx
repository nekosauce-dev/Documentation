---
title: "Get Sources"
api: "GET https://nekosauce.org/api/sources"
description: "This endpoint returns all available sources."
---

### Response

<ResponseField name="data" type="array[object]">
  Contains all the results.

  <Expandable title="data">
    <ResponseField name="id" type="number">
      The source's ID.
    </ResponseField>
    <ResponseField name="module" type="string">
      The internal import module. Not useful for API clients but it's there for internal use.
    </ResponseField>
    <ResponseField name="name" type="string">
      The human-readable name of the source.
    </ResponseField>
    <ResponseField name="urls" type="object">
      The source's URLs.

      <Expandable title="urls">
        <ResponseField name="website" type="string">
          The source's official website URL.
        </ResponseField>
        <ResponseField name="documentation" type="string">
          The source's official API documentation.
        </ResponseField>
      </Expandable>
    </ResponseField>
    <ResponseField name="enabled" type="boolean">
      Whether the source is enabled or not. If `false`, the source won't be updated with new sauces.
    </ResponseField>
  </Expandable>
</ResponseField>


<RequestExample>

```bash Example Request
curl --location --request GET 'https://api.mintlify.com/api/user' \
--header 'Content-Type: application/json' \
--header 'Authorization: ApiKey <api-key>'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "data": [
    {
      "id": 1,
      "module": "nekosauce.sauces.sources.danbooru",
      "name": "Danbooru",
      "urls": {
        "website": "https://danbooru.donmai.us/",
        "documentation": "https://danbooru.donmai.us/wiki_pages/help:api"
      },
      "enabled": true,
      "components": {
        "fetcher": "DanbooruFetcher",
        "downloader": "DanbooruDownloader",
        "tagger": "DanbooruTagger"
      },
      "sauce_types": [
        1,
        2
      ],
      "update_frequency": "default"
    },
  ]
}
```

</ResponseExample>
